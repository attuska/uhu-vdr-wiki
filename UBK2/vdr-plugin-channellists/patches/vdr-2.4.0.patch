diff -Nur orig/channellists.c mod/channellists.c
--- orig/channellists.c	2013-03-19 11:47:14.000000000 +0100
+++ mod/channellists.c	2018-05-03 08:50:33.060382502 +0200
@@ -341,6 +341,7 @@
 
 cString cPluginChannellists::SVDRPCommand(const char *Command, const char *Option, int &ReplyCode) {
   // Process SVDRP commands this plugin implements
+  LOCK_CHANNELS_READ;
   if (strcasecmp(Command, "LIST") == 0) { //   TODO need a function for reloading channellists osd
      return cString::sprintf("sorry, this is for testing only");
      }
@@ -350,7 +351,7 @@
         FILE *in,*out;
         in=fopen(Option,"r");
         if (in) {
-           Channels.SwitchTo(1);
+           Channels->SwitchTo(1);
            TimerList timers;
            timers.SaveTimer();
            out=fopen(ChannelsConf,"w");
@@ -364,8 +365,8 @@
               LOG_ERROR_STR(ChannelsConf);
               }
            fclose(in);
-           Channels.Load(ChannelsConf);
-           Channels.SwitchTo(1);
+           Channels->Load(ChannelsConf);
+           Channels->SwitchTo(1);
            timers.RestoreTimer();
            return cString::sprintf("channels.conf successfully reloaded");
            }
@@ -377,8 +378,8 @@
      else {
         TimerList timers;
         timers.SaveTimer();
-        Channels.Load(ChannelsConf);
-        Channels.SwitchTo(1);
+        Channels->Load(ChannelsConf);
+        Channels->SwitchTo(1);
         timers.RestoreTimer();
         return cString::sprintf("channels.conf successfully reloaded");
         }
@@ -391,24 +392,26 @@
 // Timer Save and Restore Methods
 void TimerList::SaveTimer() {
   myTimers.Clear();
-  for (cTimer *t = Timers.First(); t; t= Timers.Next(t)) {
+  LOCK_TIMERS_WRITE;
+  for (const cTimer *t = Timers->First(); t; t= Timers->Next(t)) {
      myTimers.Add(new TimerString(t->ToText(true)));
      }
-  Timers.cList<cTimer>::Clear();
+  Timers->cList<cTimer>::Clear();
   }
 
 // restore Timers, delete Timers with unknown channel
 void TimerList::RestoreTimer() {
-  Timers.cList<cTimer>::Clear();
+  LOCK_TIMERS_WRITE;
+  Timers->cList<cTimer>::Clear();
 
   for (TimerString *timer = myTimers.First(); timer; timer= myTimers.Next(timer)) {
      cString t = timer->GetTimerString();
      cTimer *tim = new cTimer();
      tim->Parse(t);
      if (tim->Channel() != NULL)
-        Timers.Add(tim);
+        Timers->Add(tim);
      }
-  Timers.SetModified();
+  Timers->SetModified();
   myTimers.Clear();
   }
 
@@ -565,8 +568,9 @@
 
      FILE *in,*out;
      in=fopen(ToLoad,"r");
+     LOCK_CHANNELS_READ;
      if (in) {
-        Channels.SwitchTo(1);
+        Channels->SwitchTo(1);
         TimerList timers;
         timers.SaveTimer();
         out=fopen(ChannelsConf,"w");
@@ -581,8 +585,8 @@
            ErrorMsg(trNOOP("An error occurs, see syslog..."));
            }
         fclose(in);
-        Channels.Load(ChannelsConf);
-        Channels.SwitchTo(1);
+        Channels->Load(ChannelsConf);
+        Channels->SwitchTo(1);
         timers.RestoreTimer();
         if (ActionAfterSwitch == 1)
            state = osBack;
